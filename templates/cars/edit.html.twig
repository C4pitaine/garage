{% extends "base.html.twig" %}

{% block title %}Modification{% endblock %}

{% form_theme myForm 'cars/_collection.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="my-3">Modification d'une voiture</h1>

        {{form_start(myForm)}}

            {{form_widget(myForm)}}

            <button type="submit" class="btn btn-warning my-3">Modifier</button>

        {{form_end(myForm)}}
    </div>
{% endblock %}

{% block javascripts %}
    {{encore_entry_script_tags('cars')}}
{% endblock %}