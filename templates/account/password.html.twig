{% extends "base.html.twig" %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="container full">
    <h1 class="my-2">Modifier le mot de passe</h1>
        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-success my-3">Confirmer la modification</button>

        {{form_end(myForm)}}
    </div>

{% endblock %}