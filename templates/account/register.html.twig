{% extends "base.html.twig" %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <div class="container full">
    <h1 class="my-2">Inscription</h1>
        {{form_start(myForm)}}

            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-success my-3">Inscription</button>

        {{form_end(myForm)}}
    </div>

{% endblock %}